# 🧠 Threads Scraper (Python)

A simple and lightweight scraper for **Threads (Meta)** built entirely in Python.  
It collects public posts by keywords — including nickname, text, likes, reposts, comments, language, and the keyword itself.

---

## ⚙️ Features

- Search Threads posts by any keywords  
- Extract text, author, likes, reposts, and comments  
- Detect language (Kazakh / Russian / Mixed / Unknown)  
- Save results to **CSV** and **JSON**  
- Adjustable page loading time for more results  
- Works fully standalone — no API keys required

---

## 🚀 Installation

```bash
git clone https://github.com/yourname/threads_scraper.git
cd threads_scraper
pip install -r requirements.txt
playwright install
